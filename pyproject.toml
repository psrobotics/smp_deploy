[project]
name = "smp-hw-tools"
version = "0.1.0"
description = "Host-side tooling for the smp_hw G1 controller: contract checks and sim2sim drivers."
requires-python = ">=3.10"
dependencies = [
  "onnx>=1.16",
  "pyyaml>=6.0",
  "lcm>=1.5",
]
